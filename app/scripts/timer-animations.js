window.addEventListener("load", function(event) {
 /* let segment_divider = document.getElementById("segment_divider").contentDocument;
  let divider = segment_divider.getElementsByClassName("divider");


    //divider[0].style.fill = "#11b20e";
 
    //divider[0].style.fill = "#333031";
  var TimeLine = new TimelineLite({});
  var x = function blink(){
      TimeLine.to(divider, 0.2, {fill: "#11b20e", onComplete: 
        function()
        { 
          TimeLine.to(divider, 0.5, {fill: "#333031", onComplete: x}, "+=0.5");
        }});
      
   }

  x();*/
 })